/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    // allow larger payloads for base64 screenshot uploads to server actions / route handlers
  },
};

export default nextConfig;
